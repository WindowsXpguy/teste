console.log("Backend script loaded from GitHub Pages!");

function backendFunction() {
    document.getElementById("output").innerText = "Hello from the external script!";
}

// Run the function automatically when the script loads
backendFunction();
